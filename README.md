# Chatbot_with_pdf_and_websearch
A Streamlit app that lets you chat with your PDF documents. Uses RAG (Retrieval-Augmented Generation) with ChromaDB and HuggingFace embeddings to answer questions from the PDF. Automatically falls back to web search when the answer isn't found in the document, using semantic similarity scoring to decide the routing.
